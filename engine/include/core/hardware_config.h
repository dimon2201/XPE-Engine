#pragma once

namespace xpe {

    namespace core {

        struct ENGINE_API HardwareConfig final {
            static int K_TEXTURE_2D_ARRAY_SIZE_MAX;
            static int K_ANISOTROPY_LEVEL_MAX;
        };

    }

}